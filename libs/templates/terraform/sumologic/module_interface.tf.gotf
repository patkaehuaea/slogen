terraform {
    required_providers {
        datadog = {
            source = "DataDog/datadog"
        }
        sumologic = {
            source = "sumologic/sumologic"
        }
    }
}

{{range $var := . }}

variable "{{$var}}" {
    type = string
}

{{end}}
